#include "main.h int main ()
/**
 *  * print_alphabet - prints alphabet
 *   *
 *    * Return: always 0;
*/
void print_alphabet(void); 
{
	char 

	for(ch = 'a' ; ch <= 'z' ; ch++)
	{
		putchar(ch);
	}
	return(0);
}
